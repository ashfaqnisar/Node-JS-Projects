var x= 45;
var y=30;
var type_y= typeof y//We are storing type of the variable in the type_y
var type = typeof 5//We can also store the type directly in the variable\
var z="3"	 //By default this is a number but as we are storing it in quotes it is going
 			// to declared as string
console.log("The value of x is "+x)
console.log("The value of y is "+y)
console.log(typeof y)//It gives the what is the  type of data stored in the var
console.log(type) //It is going to give the output as number not integer.
console.log(type_y)//This is also possible
console.log(typeof z);//This gives output as string

